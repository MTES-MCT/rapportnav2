extend type Query {
  agents: [Agent]!
  agentsByServiceId(serviceId: ID): [Agent]
}
type Agent {
    id: ID!
    firstName: String!
    lastName: String!
    services: [Service]
}

type Service {
  id: ID!
  name: String!
}
