name: "Release"

on:
  push:
    branches: ["main"]
  # release:
  #   types: [published]

jobs:
  publish-release:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up Docker Buildx
        id: buildx
        uses: docker/setup-buildx-action@v1

      - name: Extract Tag Name
        id: extract_tag
        run: echo "::set-output name=tag::${GITHUB_REF#refs/tags/}"

      - name: set env VERSION
        id: version
        # run: export VERSION=${{ steps.extract_tag.outputs.tag }}
        run: export VERSION=0.0.0.2

      # - name: Build Docker Image
      #   id: build-image
      #   env:
      #     GITHUB_SHA: ${{ github.sha }}
      #     VERSION: 0.0.0.2
      #     # VERSION: ${{ steps.extract_tag.outputs.tag }}
      #     ENV_PROFILE: "prod"
      #   # run: |
      #   # docker buildx build \
      #   #   --file infra/docker/app/Dockerfile \
      #   #   --tag rapportnav-app:${{ steps.extract_tag.outputs.tag }} \
      #   #   .
      #   run: make docker-build-app

      # - name: Upload image to artifacts
      #   uses: ishworkh/container-image-artifact-upload@v1.0.0
      #   with:
      #     # image: rapportnav-app:${{ steps.extract_tag.outputs.tag }}
      #     image: rapportnav-app:0.0.0.2

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build image
        uses: docker/build-push-action@v4
        env:
          GITHUB_SHA: ${{ github.sha }}
          VERSION: 0.0.0.2
          # VERSION: ${{ steps.extract_tag.outputs.tag }}
          ENV_PROFILE: "prod"
        with:
          context: .
          # load: true
          builder: ${{ steps.buildx.outputs.name }}
          file: infra/docker/app/Dockerfile
          push: true
          tags: ghcr.io/mtes-mct/rapportnav2/rapportnav-app:${{ env.VERSION }}
          build-args: |
            VERSION=${{ env.VERSION }}
            ENV_PROFILE=${{ env.ENV_PROFILE }}
            GITHUB_SHA=${{ github.sha }}

      # - name: Tag Docker Image to GitHub Packages
      #   run: make docker-tag-app
      #   env:
      #     GITHUB_SHA: ${{ github.sha }}
      #     VERSION: 0.0.0.2
      #     # VERSION: ${{ steps.extract_tag.outputs.tag }}
      #     ENV_PROFILE: "prod"

      # - name: Push Docker Image to GitHub Packages
      #   # run: make docker-push-appdocker push ${{ github.repository }}:${{ steps.extract_tag.outputs.tag }}
      #   run: make docker-push-appdocker push ${{ github.repository }}:0.0.0.2
